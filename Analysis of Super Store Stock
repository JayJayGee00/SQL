SELECT item_name, price
FROM superstore
ORDER BY price
;

SELECT AVG(price) AS "Average Product Price"
FROM superstore
;

SELECT item_name, stock_quantity, category
FROM superstore
WHERE category="Kitchen Supplies"
;

SELECT category, COUNT(DISTINCT item_name) AS "Number of Products"
FROM superstore
Group BY category
;
